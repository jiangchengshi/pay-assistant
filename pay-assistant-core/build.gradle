description("核心包")

dependencies {
    api project(":pay-assistant-annotation")

    implementation("org.springframework.boot:spring-boot-autoconfigure")
    api("com.squareup.okhttp3:okhttp:${depVersion.okhttp}")
    api("com.alibaba.fastjson2:fastjson2:${depVersion.fastjson2}")
}

test {
    useJUnitPlatform()
}